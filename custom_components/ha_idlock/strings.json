{
  "config": {
    "step": {
      "user": {
        "title": "Select ID Lock devices",
        "data": {
          "locks": "ZHA lock entities"
        }
      }
    },
    "abort": {
      "already_configured": "Already configured. Only a single instance is supported — use Configure to add or remove locks."
    },
    "error": {
      "no_valid_locks": "Select at least one valid ZHA lock entity."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "ID Lock Settings",
        "data": {
          "locks": "ZHA lock entities"
        }
      }
    }
  }
}
